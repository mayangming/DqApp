<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@+id/layout"
                xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content">

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/video_item_icon"
            android:layout_width="90dp"
            android:layout_height="160dp"
            android:scaleType="fitXY"
            android:background="@drawable/qc_video_bg"/>

        <ImageView
            android:id="@+id/video_item_play"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center"
            android:src="@mipmap/cn_player_center"
            android:visibility="gone"/>

        <TextView
            android:id="@+id/video_item_duration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="right|bottom"
            android:layout_marginBottom="6dp"
            android:layout_marginRight="6dp"
            android:src="@mipmap/cn_player_center"
            android:text="00:00"
            android:textColor="@color/white"/>

        <com.meetqs.qingchat.carema.view.CircleProgressView
            android:id="@+id/video_item_progress"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center"
            android:visibility="gone"/>
    </FrameLayout>

</RelativeLayout>
